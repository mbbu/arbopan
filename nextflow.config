// Global default params, used in configs
params {
    // Input options
    input = null

    // Output options
    outdir = "./results"
    publish_dir_mode = "copy"

    // Mafft options
    threads = -1
}


profiles {
    conda {
        process.conda = "$projectDir/environment.yml"
    }
}